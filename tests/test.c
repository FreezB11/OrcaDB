

int main(){
    // here what happens 
    // one main thread to launch the whole application
    // we use other threads for shards
    // so the main(one) thread will send the info
    // to the respective thread
}